name: Full Flutter CI

on:
  # Temporarily manual-only while the Cyberdriver MVP release path is Windows
  # service MSI focused. Keep this upstream RustDesk workflow available for
  # future full Flutter/platform validation.
  workflow_dispatch:

jobs:
  run-ci:
    uses: ./.github/workflows/flutter-build.yml
    with:
      upload-artifact: false
