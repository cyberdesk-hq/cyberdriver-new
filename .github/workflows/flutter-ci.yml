name: Cyberdriver Windows CI

on:
  # Windows-only for launch. Keep the reusable full Flutter matrix in
  # flutter-build.yml for future macOS/Linux/mobile validation.
  workflow_dispatch:

jobs:
  build-windows-msi:
    uses: ./.github/workflows/release-windows-msi.yml
    secrets: inherit
    with:
      release_tag: "ci"
