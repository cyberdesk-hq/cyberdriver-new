name: Flutter Nightly Build

on:
  # Disabled for launch: Cyberdriver release automation is Windows MSI only.
  # Re-enable the full Flutter matrix when macOS/Linux/mobile artifacts are supported again.
  workflow_dispatch:

jobs:
  build-windows-msi:
    uses: ./.github/workflows/release-windows-msi.yml
    secrets: inherit
    with:
      release_tag: "nightly"
