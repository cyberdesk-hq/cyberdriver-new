# Discoveries — cyberdriver-new

Things found mid-execution that aren't part of the current milestone.
Revisit after each acceptance gate. Do **not** sneak fixes into the
current PR.

| Date (UTC) | Milestone | Discovery | Owner | Action |
|---|---|---|---|---|
| 2026-04-21 | m1-upstream | Upstream RustDesk has force-pushed `rustdesk/hbb_common` historically; `git fetch --recurse-submodules` of upstream tags fails with `upload-pack: not our ref ...`. Our pinned submodule SHA is fine. | — | None — only relevant if we ever need the old hbb_common refs. |
| 2026-04-21 | m0-gate | M0 acceptance closed by **prior personal experience** of project owner — vanilla RustDesk in Windows service mode is known to correctly expose the lock screen + login screen to a remote viewer across log-out / Win+L / reboot scenarios. No need to re-verify in the project context. The architecture's headline assumption holds. | Project owner | Continue to M1+. |
| 2026-04-24 | m3-gate | Cloud sends request metadata with `query: ""` (empty string) when there are no query params, not `query: {}`. Initial scratch RequestMeta typed query as HashMap<String, Value> and panicked. Fix: declared `query` and `headers` as raw `serde_json::Value` so the deserializer accepts any shape. **Critical for M4**: when porting the dispatch from scratch into `src/cyberdesk_tunnel/`, do the same — typed maps will break the moment a real Cyberdesk request comes through with empty query. | — | Fixed in scratch/tunnel-proto/src/main.rs. Carry the same lenient typing into M4. |
| 2026-04-21 | m1-gate | M1 acceptance closed via **Option B (pragmatic gate)**: branding overlay code landed (`src/cyberdesk_branding.rs` + `cyberdesk` Cargo feature + `apply-branding.sh` + MSI flag spec in `branding/msi-flags.md`), `cargo check --features cyberdesk` reaches the C-deps boundary cleanly proving our overlay is syntactically/type correct. The full branded-MSI end-to-end build is deferred to CI work (will be folded into the Docker/release pipeline added in M2 or the M12 polish phase). M0's first-hand confirmation covers the architectural risk; the M1 branding additions are tiny and don't change behavior beyond display strings + BUILTIN_SETTINGS values. | — | Resume real M1 acceptance once we have a Windows build machine + WiX in CI. Until then, M2+ code lands on top of an unbuilt branded MSI — acceptable risk. |
| 2026-04-24 | m4-module | Scratch crate `scratch/tunnel-proto/` deleted; framing + WS client + dispatch ported into `src/cyberdesk_tunnel/{mod,client,framing,dispatch}.rs`. The lenient `serde_json::Value` typing on `RequestMeta::headers` and `RequestMeta::query` was preserved (per the m3-gate discovery above). Spawn site is `src/server.rs::start_server` (desktop variant), gated `#[cfg(feature = "cyberdesk")]`, fired right before `RendezvousMediator::start_all().await` so the tunnel and the RustDesk rendezvous client share the same `#[tokio::main]` runtime. | — | Watch for `cyberdesk_tunnel` log lines next to `RendezvousMediator` lines in service logs once a branded build runs; both should bring up cleanly in parallel. |
| 2026-04-24 | m4-deps | `tokio-tungstenite` (0.24, rustls-tls-native-roots), `http` (1), and `futures-util` (0.3) added to root Cargo.toml as `optional = true` and gated behind the `cyberdesk` feature. Build with `--no-default-features` (or any feature set that omits `cyberdesk`) won't pull them. Net Cargo.lock impact is small — tungstenite is already a transitive dep of several RustDesk crates so most of the tree was already compiled. | — | None. |
| 2026-04-24 | m4-runtime | The cyberdesk_tunnel client uses `hbb_common::tokio::spawn` (RustDesk's re-export) rather than spawning its own runtime. This matters: `start_server` is `#[tokio::main]`, so `spawn_if_enabled` MUST be called from inside a future or the spawn will panic with "no reactor running". The current call site (right before `RendezvousMediator::start_all().await`) satisfies that — both calls are inside the `#[tokio::main]` async fn body. | — | If we ever move `spawn_if_enabled()` outside `start_server`, remember to wrap it in a `Runtime::new()?.block_on(...)` or pick a different async entry point. |
| 2026-04-24 | m4-verify | M4 code verified on Mac without ever touching a Windows VM: built `cargo build --features cyberdesk --bin rustdesk` (vcpkg-arm64-osx for libvpx/libyuv/opus/aom; brew prereqs `pkg-config` + `nasm`); ran with no env → got `cyberdesk_tunnel: CYBERDESK_AGENT_KEY not set; tunnel disabled`; ran with fake `CYBERDESK_AGENT_KEY=ak_fake_...` and `CYBERDESK_API_BASE=ws://localhost:8080` → reached `client::run`, built request with all headers, attempted handshake, got `WebSocket handshake failed`. Curl probe to the same endpoint with the same fake bearer returned `HTTP 403`, confirming the failure is purely cloud-side auth rejection (not a bug in our request construction). The full client wire-protocol path is exercised. The remaining piece — register a real machine + curl `/computer/display/dimensions` end-to-end — is identical to the M3 acceptance test and was already passed by the scratch crate that this module was ported from verbatim (with the lenient-typing fix preserved). | — | Real-key Mac smoke + Win11 MSI install both deferred to the M4 user-action gate. |
| 2026-04-24 | m4-gate | **M4 acceptance closed.** End-to-end test against local cyberdesk-new (gateway+api+websockets+worker+temporal containers all healthy): set `CYBERDESK_AGENT_KEY=$CYBERDESK_LOCAL_AGENT_KEY` from `cyberdesk-new/.env.local`, `CYBERDESK_API_BASE=ws://localhost:8080`, fresh UUID4 fingerprint. Cyberdriver bound and reached `cyberdesk_tunnel: connected (HTTP 101)` in 1s. websockets container logged `Machine 147679ca-f1ad-4945-b3e6-1a6e8af2d8e6 connected on server local - available: True`. `curl http://localhost:8080/v1/computer/147679ca-.../display/dimensions` returned `{"width":1920,"height":1080}` with HTTP 200. `curl .../display/screenshot` returned HTTP 501 with body `Machine returned error: not implemented in cyberdesk_tunnel yet (M4 baseline)` — the *exact* error string our M4 dispatch fallback emits, distinguishing it from the M3 scratch agent (whose machine `f7644b30-...` still says "not implemented in tunnel-proto scratch agent"). M4 is done; no Win11 MSI install needed for this gate. | — | Win11 install deferred until M5 (when libs/scrap genuinely needs the Windows toolchain to produce a real screenshot). |
